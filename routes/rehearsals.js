const express = require('express');
const router = express.Router();
// const rehearsalController = require('../controllers/rehearsalController');

// Placeholder route
router.get('/', (req, res) => {
  res.send('Get rehearsals route');
});

module.exports = router;